# Break Timer Android App

Native Android WebView break timer app with GitHub Actions APK build.

## Build APK

Open the **Actions** tab, run **Build Android APK**, then download the `BreakTimer-debug-apk` artifact.
